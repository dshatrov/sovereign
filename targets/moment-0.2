# NAME			VERSION			FLAGS

linux-headers		linux-headers-2.6.36.2

# glibc-2.14 fails to build, and building binutils first gives nothing.
#
#binutils		binutils-2.21.1a_pass2
#glibc			glibc-2.14.1

# FIXME Why not glibc-2.14?
#       Because it does not build for some reason (ld bugs).
# UPD: glibc-2.14 requires binutils-2.21.1a (is binutils-2.21.1 enough?)
glibc			glibc-2.12.2

zlib			zlib-1.2.5
#glib			glib-2.28.8

libffi			libffi-3.0.10

glib			glib-2.30.2

